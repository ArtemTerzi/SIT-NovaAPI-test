export const selectPhone = ({ theme: { media } }) => media.phone;
export const selectTablet = ({ theme: { media } }) => media.tablet;
export const selectDesktop = ({ theme: { media } }) => media.desktop;
